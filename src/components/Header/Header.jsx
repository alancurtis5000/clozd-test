const Header = () => {
  return (
    <div
      className="Header"
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <h1
        style={{
          margin: 0,
        }}
      >
        Alan Curtis: Get all the Users
      </h1>
    </div>
  );
};

export default Header;
